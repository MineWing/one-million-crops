# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                   ONE MILLION CROPS                         ┃
# ┃      Everything here reloads with /1mill reload            ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

challenge:
  # One shared target is used for every enabled entry in crops.yml.
  season: 2
  target-per-crop: 200000

participants:
  # EVERYONE: every player contributes. Perfect for a private server.
  # ALLOWLIST: only UUIDs below contribute (names are deliberately not used).
  mode: EVERYONE
  allowlist: []

counting:
  # Legacy fallback for water/pistons only when their source keys below are absent.
  # Prefer /1m automode water, pistons or hoppers to toggle each source.
  allow-automated-farms: true
  # Separate credit controls. Disabling a source does not stop its machinery.
  automated-farms:
    water: true
    pistons: true
    hoppers: false

  # Prevents Q/drop-and-repickup loops. Keep this enabled for a fair challenge.
  block-player-redrops: true

  # Player-broken crops must be fully grown before their drops become eligible.
  require-mature-crops: true

storage:
  database-file: progress.db
  autosave-seconds: 5
  backup-before-reset: true

web:
  # The live dashboard is read-only. Keep localhost for use behind a reverse
  # proxy, or change to 0.0.0.0 to expose it directly on your LAN/server.
  enabled: true
  bind-address: 0.0.0.0
  port: 8765
  # Optional externally reachable address shown by /1mill web.
  # Example: https://crops.example.com
  public-url: ''
  refresh-ticks: 20
  history:
    sample-seconds: 30
    retention-hours: 24

scoreboard:
  enabled-by-default: true
  # Title animation speed. 1 tick renders the rainbow at 20 frames per second.
  animation-ticks: 1
  # Progress values are cheaper to refresh and do not need to animate every frame.
  refresh-ticks: 10
  page-change-ticks: 80
  crops-per-page: 7
  # Frames in one complete, seamless rotation of all gradient color stops.
  title-animation-frames: 120
  title-frames:
    - '<b><gradient:#FF8FBD:#FFC2DE:#FFE1EE>CROPS • SEASON 2</gradient></b>'

gui:
  animation-ticks: 5
  # Play a quiet pickup confirmation to the contributing player.
  pickup-sound: false

celebration:
  milestones: [25, 50, 75, 90]
  fireworks: 3
  firework-gap-ticks: 10
  completion-sound: minecraft:ui.toast.challenge_complete
  completion-volume: 1.0
  completion-pitch: 1.0

# Rivet utilities. Changes apply with /1mill reload.
timber:
  enabled: true
egg-capture:
  enabled: true
vein-mining:
  enabled: true
